#include <bits/stdc++.h>
using namespace std;
using ll =long long;
int main() {
    int n;
    cin >> n;
    vector<ll>nums(n, 0);
    for (int i = 0; i < n; ++i) cin >> nums[i];
    for (int x : nums) {
        if
    }

}